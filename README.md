# ECAI - Education Consultants Association of India

This is the official frontend application for the Education Consultants Association of India (ECAI), built with a modern, futuristic 2026 design aesthetic.

## 🚀 Tech Stack

- **Framework**: Angular 19+ (Standalone Components)
- **Styling**: Tailwind CSS v3
- **Design**: Glassmorphism, Premium Gradients, Outifit Typography
- **Icons**: Lucide Angular & Custom SVGs
- **Deployment**: Optimized for Vercel

## 🛠️ Getting Started

### Prerequisites

- Node.js (v20+)
- npm

### Installation

1. Clone the repository
2. Navigate to the project folder: `cd frontend`
3. Install dependencies:
   ```bash
   npm install
   ```

### Development Server

To start the local development server:
```bash
npm start
```
Navigate to `http://localhost:4200/`.

### Building for Production

To generate a production build:
```bash
npm run build
```
The build artifacts will be stored in the `dist/ecai-app` directory.

## 📁 Project Structure

- `src/app/components`: Reusable UI components (Navbar, Footer, etc.)
- `src/app/pages`: Main application views and sub-pages
- `src/styles.css`: Global styles, design tokens, and Tailwind directives
- `public/`: Static assets (images, icons)

## 🌐 Deployment on Vercel

This project includes a `vercel.json` for SPA routing support. Vercel will automatically detect the Angular framework and apply the correct build settings.

---
&copy; 2026 Education Consultants Association of India. All rights reserved.
